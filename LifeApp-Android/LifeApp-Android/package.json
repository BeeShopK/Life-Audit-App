{
  "name": "lifeapp",
  "version": "2.0.0",
  "description": "LifeApp — Know Yourself. A self-awareness PWA wrapped as an Android app.",
  "main": "index.js",
  "scripts": {
    "build": "echo 'Static app — no build step needed'",
    "android": "npx cap sync android && npx cap open android",
    "sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
